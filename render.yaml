services:
  - type: web
    name: statready-ai
    runtime: docker
    region: frankfurt
    plan: starter
    autoDeployTrigger: commit
    healthCheckPath: /_stcore/health
    dockerfilePath: ./Dockerfile
